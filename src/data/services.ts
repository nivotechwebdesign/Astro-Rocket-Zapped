/**
 * Services content for the current client site.
 *
 * Stub for a later phase. When a dedicated services page is built, its content
 * will live here and be passed into the approved section variants — the same
 * pattern used by `home.ts`. Left intentionally empty for now.
 */

export {};
